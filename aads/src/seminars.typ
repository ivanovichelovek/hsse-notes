// Манифест семинаров: порядок #include здесь задаёт порядок в собранной книге.
// Каждый семинар — отдельный файл в seminars/, самодостаточный (сам
// импортирует преамбулу), поэтому его можно компилировать и по одному
// через build-one.sh.

#include "seminars/01.typ"
